//rccp권장, rafce, rce, rcep

import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class MyColor extends Component {
    static propTypes = {
        prop: PropTypes
    }

    render() {
        return (
            <div>
                
            </div>
        )
    }
}

